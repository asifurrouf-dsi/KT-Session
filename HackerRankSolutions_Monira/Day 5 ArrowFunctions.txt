 */
function modifyArray(nums) {
    for (let i = 0; i < nums.length; i++){
        if (isEven(nums[i])) nums[i] = nums[i] * 2;
        else nums[i] = nums[i] * 3;
    }
    
    return nums;
}

const isEven = (num) => num % 2 === 0;
